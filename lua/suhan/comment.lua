require('Comment').setup {
    -- Optional: customize mappings
    toggler = {
        line = '<C-_>',  -- Use Ctrl + / (Ctrl + _ is the same keybinding)
    },
    opleader = {
        line = '<C-_>',
    },
}

